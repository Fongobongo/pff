# Copy to .env.local for development

# Database (recommended: Supabase Postgres free tier)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

# Base (Ethereum L2) RPC
# Must be free-tier. Options:
# - a public Base RPC
# - a provider with a free tier (e.g., Alchemy)
BASE_RPC_URL=https://mainnet.base.org

# Optional: third-party API keys for player stats sources
# STATS_API_KEY=
