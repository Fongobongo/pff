# Copy to .env.local for development

# Database (recommended: Supabase Postgres free tier)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

# Supabase REST (required for Sport.fun price store reads/writes)
SUPABASE_PROJECT_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Base (Ethereum L2) RPC
# Must be free-tier. Options:
# - a public Base RPC
# - a provider with a free tier (e.g., Alchemy)
BASE_RPC_URL=https://mainnet.base.org

# Blockchain explorers
# Etherscan API v2 (note: free-tier may not cover Base mainnet)
ETHERSCAN_API_KEY=

# Base wallet history (recommended: Alchemy free tier)
ALCHEMY_API_KEY=

# Optional: third-party API keys for player stats sources
# STATS_API_KEY=

# StatsBomb storage mode:
# - hybrid: local cache + KV + Postgres
# - db: Postgres only (no KV)
STATSBOMB_STORAGE_MODE=hybrid

# Optional: enable external Sleeper projections for NFL (0/1)
SLEEPER_PROJECTIONS_ENABLED=0

# Optional: JSON override for FUN reward tiers (array of FunRewardTier)
# FUN_REWARD_TIERS_JSON=[{"minBalance":0,"holdingScore":0,"weeklyBonusTp":0,"seasonFunMultiplier":1}]
FUN_REWARD_TIERS_JSON=

# Optional: fallback source for NFL player token metadata (name/team/position)
NFL_FUN_PLAYERS_DATA_URL=https://nfl-fun.vercel.app/data/players/players.json

# Optional: warn (throttled) when unresolved metadata share in NFL market API exceeds this percent
NFL_MARKET_UNRESOLVED_ALERT_PCT=25

# Optional: lightweight market-alert sink retention (used by /api/sportfun/market-alerts)
MARKET_ALERT_SINK_MAX=300
MARKET_ALERT_RETENTION_HOURS=168

# Required to mutate alerts (ack/mute/unmute) via POST /api/sportfun/market-alerts
# Keep this secret and configure only in trusted environments.
MARKET_ALERT_ADMIN_TOKEN=

# Sport.fun external price sync (GeckoTerminal + DexScreener)
SPORTFUN_PRICE_SYNC_ENABLED=1
SPORTFUN_PRICE_REFRESH_MINUTES=10
# Optional extra token addresses for external sync (comma-separated)
SPORTFUN_EXTERNAL_PRICE_TOKENS=
# Optional Sport.fun app API config (used for strict TP parity from in-game player records)
SPORTFUN_APP_API_BASE_URL=https://app.sport.fun/api
SPORTFUN_AUTH_BEARER_TOKEN=
SPORTFUN_GAME_TP_SYNC_INTERVAL_MINUTES=10

# Optional override for Tenero API base URL
TENERO_API_BASE_URL=https://api.tenero.io
# Optional bearer token for auth-gated Tenero endpoints (tracked wallets / remarks / portfolio wallets)
TENERO_AUTH_BEARER_TOKEN=
